# Get Next Line

Compilation:

 `make all`

Running

`./test_gnl "file_path"`

**Return value:**
* `return 1 when it read a line`
* `return 0 when it finished reading a file`
* `return -1 when an error occurs`
